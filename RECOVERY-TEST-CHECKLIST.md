# ✅ RECOVERY TEST CHECKLIST — ยืนยัน redesign v2 กลับครบ (refresh ดูจริง)

> หลังกู้ (ดู `RECOVERY-NOTES.md`) · เปิด web-admin refresh แล้วไล่เช็ค · **เน้นจุด ⚠️ = reconstruct ไม่เป๊ะต้นฉบับ**
> ล็อกอิน: `admin@ros.local` / `ChangeMe!2026` · หรือ 7 บัญชีทดสอบ `test.{role}@ros.local`/`abcd1234`

## 🎨 ภาพรวมดาร์ก (D5 — ควรกลับมาเหมือนเดิม)
- [ ] เปิดแอปครั้งแรก = **ดาร์ก near-black อุ่น** (ไม่ใช่สว่าง) — พิสูจน์ dark-default script ทำงาน
- [ ] การ์ด = พื้นเข้ม + **กรอบ subtle** (ไม่พึ่งเงา) · สลับ light ที่ ThemeToggle ได้
- [ ] ปุ่มหลัก = **ทอง** ตัวอักษรเข้ม · ปุ่มรอง = โปร่งกรอบ hover ยกชั้น

## 🔩 primitives (D6/D14 — ไฟล์แกน ตรงเป๊ะ 100%)
- [ ] Toast = การ์ดดาร์ก + **กรอบสีตามชนิด** (แดง/เขียว) มุมโค้งใหญ่ (xl2)
- [ ] เปิด ⋯ / dropdown / Combobox = มุมโค้งใหญ่เท่า modal · hover แถว = **ยกชั้น** (ไม่เข้มลง)
- [ ] InfoGroup (กล่องข้อมูล detail) = มุมโค้ง 16 เท่าการ์ด · seg (ตัวกรอง) เลือกแล้ว = **มีกรอบชัด**

## 🐚 shell / accent (D13 — ⚠️ บางจุด reconstruct)
- [ ] ⚠️ **โลโก้ R** (มุมซ้ายบน sidebar) = ช่องทอง ตัว R เข้ม — *ผมเดาว่าช่องทอง; ต้นฉบับอาจต่าง (เช่นตัว R ทองบนช่องเข้ม)*
- [ ] nav เมนูที่เลือกอยู่ = **ยกชั้น + ตัวอักษรทอง** (ไม่ใช่แถบขาวจ้า)
- [ ] ⚠️ **ปฏิทิน** วันที่เลือก = ช่องทอง · จุดใต้วัน = *ผมทำเป็นจุดเข้ม; ต้นฉบับอาจเป็นจุดขาว*
- [ ] chip เลือก (ประเภททรัพย์ / หมวดแจ้งเตือน) = **โทนทองจาง** (gold-tint)
- [ ] ⚠️ **stepper ฟอร์มเพิ่มทรัพย์** = ขั้นปัจจุบันวงทอง · ขั้นที่ผ่านวงทองจาง — *ผม reconstruct ให้เข้าธีม*

## 🩹 dark-leak (D15 — ต้อง 0 จุดขาวจ้า)
- [ ] แกลเลอรีรูปทรัพย์: ปุ่ม ‹ › + "ตั้งเป็นปก" + เลขหน้า = **scrim ดำ** บนรูป (ไม่ใช่ปุ่มขาว)
- [ ] เปิดรูปเต็มจอ (Lightbox) = พื้นหลัง**ดำ** (ไม่ใช่ขาว)
- [ ] มือถือเปิดเมนู drawer = พื้นหลัง**หรี่ลง** (ไม่สว่างขึ้น)
- [ ] badge "ปก" · ตัวเลขกระดิ่งแจ้งเตือน · amenity chip = ตัวอักษร**เข้มบนทอง** (อ่านชัด)

## ♿ a11y light-mode (D16)
- [ ] สลับ **light mode** → ราคา/ลิงก์/nav ทอง = เข้มพออ่านชัด (gold `#8C6E42`)
- [ ] Avatar (ลิสต์ลูกค้า/เจ้าของ) = light วงเข้ม · **dark เทา muted ไม่ขาวจ้า**

## 🧪 ระบบยังทำงาน (ไม่ใช่แค่หน้าตา)
- [ ] ล็อกอิน → dashboard โหลด KPI · เปิด property/lead/contract detail ได้
- [ ] สร้าง/แก้ข้อมูล 1 อย่าง (เช่นเพิ่ม lead) → บันทึกได้ · toast เด้ง
- [ ] 3 แอป typecheck เขียว (`npx tsc --noEmit` ใน apps/web-admin) — **ยืนยันแล้ว = 0**

---
**ถ้าจุด ⚠️ ดูไม่ตรงใจ** → บอกผมค่าที่อยากได้ ผมปรับให้ตรง (logo/calendar-dot/stepper เป็น 3 จุดเดียวที่ไม่ 100%)
